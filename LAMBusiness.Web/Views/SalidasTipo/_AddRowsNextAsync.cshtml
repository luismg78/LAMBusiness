@model IEnumerable<LAMBusiness.Shared.Catalogo.SalidaTipo>

@if (Model.Any())
{
    foreach (var item in Model)
    {
        <div class="row bg-row">
            <div class="border-bottom col-10 col-md-9 p-3">
                <span class="text-dark">@Html.DisplayFor(modelItem => item.SalidaTipoDescripcion)</span>
            </div>
            <div class="border-bottom col-2 col-md-3 px-3 py-2 text-right">
                @if (Convert.ToBoolean(ViewBag.PermisoEscritura))
                {
                    <a asp-action="Edit" asp-route-id="@item.SalidaTipoID" class="d-block d-md-inline text-decoration-none">
                        <div class="btn-icon rounded-circle" data-toggle="tooltip" data-trigger="hover" data-placement="bottom" data-original-title="editar registro">
                            <i class="fa fa-edit"></i>
                        </div>
                    </a>
                    <a href="#" data-id="@item.SalidaTipoID" class="d-block d-md-inline deleteConfirm text-decoration-none" data-toggle="modal" data-target="#deleteConfirm">
                        <div class="btn-icon rounded-circle" data-toggle="tooltip" data-trigger="hover" data-placement="bottom" data-original-title="eliminar registro">
                            <i class="fa fa-trash"></i>
                        </div>
                    </a>
                }
            </div>
        </div>
    }
}