@model LAMBusiness.Shared.Movimiento.SalidaDetalle

@{
    ViewData["Title"] = "Detalle de Salidas";
}

<form asp-action="AddDetails">
    <div class="modal-content border-0 box-shadow box-responsive mx-auto">
        <div class="modal-header pb-0 align-items-baseline">
            <div>
                <h3 class="modal-title" id="exampleModalLabel">Detalle de Salidas</h3>
                <ul class="breadcrumb bg-transparent p-0 small">
                    <li class="breadcrumb-item"><a asp-action="Movimiento" asp-controller="Home">Movimientos</a></li>
                    <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Salidas">Salidas</a></li>
                    <li class="breadcrumb-item"><a asp-action="Details" asp-controller="Salidas" asp-route-id="@Model.SalidaID">Detalle de Salida</a></li>
                    <li class="breadcrumb-item active">Nuevo Registro</li>
                </ul>
            </div>
            <div class="d-flex align-items-center">
                <a class="btn-help text-secondary me-3" href="/" onclick="help('ViewCreate', event);">
                    <i class="fa fa-question-circle"></i>
                </a>
                <a asp-action="Details" asp-controller="Salidas" asp-route-id="@Model.SalidaID" class="btn-close me-md-1" aria-label="Close"></a>
            </div>
        </div>
        <div class="modal-body py-4">
            <input type="hidden" asp-for="SalidaID" />
            <div class="row px-md-3">
                <!-- Almacén -->
                <div class="col-12">
                    <div id="AlmacenSearch" class="form-group @if (Model.Almacenes != null) {<text>d-none</text>}">
                        <input asp-for="AlmacenID" type="hidden" />
                        <label asp-for="AlmacenID" class="control-label"></label>
                        <div class="input-group search-0">
                            <input id="AlmacenSearchInput" type="search" class="form-control"
                                   data-bs-toggle="focus" data-bs-target=".list-registers"
                                   placeholder="Ingrese nombre del almacén..."
                                   autofocus autocomplete="off"
                                   onkeyup="positionCursorWithArrowKey(event);"
                                   onkeydown="getAlmacen(event);" />
                            <button id="AlmacenSearchButton" class="btn btn-light border"
                                    type="button" onclick="searchAlmacen(event);">
                                <span id="SpinListAlmacen" class="d-flex align-items-center">
                                    <i class="fa fa-search"></i>
                                </span>
                            </button>
                        </div>
                        <span asp-validation-for="AlmacenID" class="text-danger"></span>
                        <div id="dRowAlmacen" class="mb-4 mt-1 list-registers" data-charging="0" data-rows-next="1">
                        </div>
                    </div>
                </div>
                <div id="AlmacenDatos" class="col-12 py-2 @if (Model.Almacenes == null) {<text>d-none</text>}">
                    <div>
                        <div class="d-flex justify-content-between align-items-center">
                            <h6 class="fw-bold text-base">Datos del almacén</h6>
                            <a href="#" onclick="deleteButtonAlmacen(event);"><span>&times;</span></a>
                        </div>
                        <hr class="my-2" />
                        <small id="Nombre" class="d-block">
                            @if (Model.Almacenes != null)
                            {
                                <text>@Model.Almacenes.Nombre</text>
                            }
                        </small>
                        <small id="Descripcion" class="text-muted">
                            @if (Model.Almacenes != null)
                            {
                                <text>@Model.Almacenes.Descripcion</text>
                            }
                        </small>
                        <hr />
                    </div>
                </div>
                <!-- Producto -->
                <div class="col-12">
                    <div id="ProductoSearch" class="form-group">
                        <input asp-for="ProductoID" type="hidden" />
                        <label asp-for="ProductoID" class="control-label"></label>
                        <div class="input-group search-0">
                            <input id="ProductoSearchInput" type="search" class="form-control"
                                   data-bs-toggle="focus" data-bs-target=".list-registers"
                                   placeholder="Ingrese código o nombre..."
                                   autofocus autocomplete="off"
                                   onkeyup="positionCursorWithArrowKey(event);"
                                   onkeydown="getProducto(event);" />
                            <button id="ProductoSearchButton" class="btn btn-light border"
                                    type="button" onclick="searchProducto(event);">
                                <span id="SpinList" class="d-flex align-items-center">
                                    <i class="fa fa-search"></i>
                                </span>
                            </button>
                        </div>
                        <span asp-validation-for="ProductoID" class="text-danger"></span>
                        <div id="dRowProducto" class="mb-4 mt-1 list-registers" data-charging="0" data-rows-next="1">
                        </div>
                    </div>
                </div>
                <div id="ProductoDatos" class="col-12 py-2 d-none">
                    <div>
                        <div class="d-flex justify-content-between align-items-center">
                            <h6 class="fw-bold text-base">Datos del producto</h6>
                            <a href="#" onclick="deleteButtonProducto(event);"><span>&times;</span></a>
                        </div>
                        <hr class="my-2" />
                        <small id="Nombre" class="d-block"></small>
                        <small id="ProductoCodigo" class="text-muted"></small>
                        <hr />
                    </div>
                </div>
                <!-- Cantidad -->
                <div class="col-12">
                    <div class="form-group">
                        <label asp-for="Cantidad" class="control-label"></label>
                        <div class="input-group search-0">
                            <span class="input-group-text">
                                <i class="fas fa-cash-register"></i>
                            </span>
                            <input asp-for="Cantidad" type="number" value="@Model.Cantidad"
                                   step="0.0001" min="0.0000" class="form-control" />
                        </div>
                        <span asp-validation-for="Cantidad" class="text-danger"></span>
                    </div>
                </div>
            </div>
        </div>
        <div class="modal-footer justify-content-between mt-4">
            <div class="px-md-3">
                <input type="submit" value="Agregar Detalle" class="btn btn-base" />
                <a asp-action="Details" asp-controller="Salidas" asp-route-id="@Model.SalidaID" class="btn btn-outline-secondary">Regresar</a>
            </div>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        </div>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        let urlAlmacen = '@Url.Action("GetAlmacenByName", "Almacenes")';
        let urlAlmacenes = '@Url.Action("GetAlmacenesList", "Almacenes")';
        let urlProducto = '@Url.Action("GetProductoByCode", "Productos")';
        let urlProductos = '@Url.Action("GetProductList", "Productos")';
    </script>
    <script src="~/js/movements.js"></script>
}
